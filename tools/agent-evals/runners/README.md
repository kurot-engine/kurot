# Runners

Model invocation and browser execution adapters will live here. A runner must
record its model, prompt context, Kurot revision, timing, token usage, and raw
artifacts without embedding credentials in reports.
