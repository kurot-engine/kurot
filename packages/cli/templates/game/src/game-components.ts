/**
 * Central export entry for custom EXML components.
 *
 * Export components used as `<game:ComponentName />` in EXML from this file.
 * This entry is configured by `exml.namespaces.game` in `kurot.config.ts`.
 */
