# Tasks

Task definitions are grouped into `basic`, `interaction`, `ui`, `gameplay`,
and `debugging`. A task is not considered active until every acceptance item
has an executable implementation or an explicitly defined visual rubric.
